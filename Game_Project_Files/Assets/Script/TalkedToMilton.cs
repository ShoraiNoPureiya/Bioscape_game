using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TalkedToMilton : MonoBehaviour
{
    public static bool _I;
    // Start is called before the first frame update
    void Start()
    {
        _I = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
