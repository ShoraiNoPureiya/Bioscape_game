using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PanelCarta : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        gameObject.SetActive(false); //Turn off the letter elements in the scene
    }

    // Update is called once per frame
    void Update()
    {

    } 
}
